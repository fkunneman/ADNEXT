#!/bin/bash

echo '----Script Begins----'

wcthreshold=$1
echo $wc_threshold
array=(299 199 99 49 29 19)


for h in {1..12}
do

python3.3 TweetStatistics.py --minframe $(($h*60)) --dayback 8 > out/out$((h*60))m149w249w &

done

wait



#-----------------------------
# for wc in "${array[@]}"
# do
# python3.3 TweetStatistics.py --minframe 60 --dayback 8 --wcount $wc > out/out60mdevset$wc &

# done
# wait
#-----------------------------
# echo 'Second Part Begins'
# # ---- second part ----

# for h in {1..18}
# do

# for i in {11..20}
# do
# python3.3 TweetStatistics.py --eventno $i --minframe $(($h*60)) --dayback 8 --wcount $wcthreshold > out$((h*60))m8d${wcthreshold}w${i}e &

# done

# wait

# done
# wait

# echo '3. Part Begins'
# # ---- second part ----

# for h in {1..18}
# do

# for i in {21..30}
# do
# python3.3 TweetStatistics.py --eventno $i --minframe $(($h*60)) --dayback 8 --wcount $wcthreshold > out$((h*60))m8d${wcthreshold}w${i}e &
# done

# wait

# done
# wait

# echo 'Third and Last part Starts'

#!/bin/bash
# declare an array called array and define 3 vales
array=( one two three )
for i in "${array[@]}"
do
	echo $i
done
